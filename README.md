The Project is a simple To-do list.

At the moment the user can create an account and delete it, change the settings. Changing the settings will automaticaly update the value in the database same for deleting and creating an account.

Adding and deleting Task is still under preparations and will be available for further updates

On the setting page the "Text size", "Language" and "Default sort" buttons are still under work so they don't work at the moment.